{
  "name": "@barchart/events-public",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "bump": "lerna version --force-publish",
    "clean": "lerna clean --yes && rm -rf node_modules"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
