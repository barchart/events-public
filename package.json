{
  "name": "@barchart/events-public",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "bump": "lerna version -m 'Release. Bump version number %v' --tag-version-prefix='' --force-publish --no-granular-pathspec",
    "clean": "lerna clean --yes && rm -rf node_modules",
    "lint": "lerna run lint --stream",
    "version": "gulp embed-version --gulpfile ./packages/clients/sdk-js/gulpfile.js"
  },
  "devDependencies": {
    "jshint": "^2.10.3",
    "lerna": "^3.22.1"
  },
  "license": "MIT"
}
